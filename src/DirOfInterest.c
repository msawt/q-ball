#include "DirOfInterest.h"
#include <math.h>

#define pi 3.14159265

double* getEquator(int k){
	int kvec[k];
	int kzeroes[k];
	for(int i = 0; i < k; ++i){
		kvec[i] = i+1;
	}
	for(int i = 0; i < k; ++i){
		kzeroes[i] = 0;
	}
	double theta[k];
	double C[k];
	for(int i = 0; i < k; ++i){
		theta[i] = (2 * pi / k) * kvec[i];
		C[i] =
	}





}

