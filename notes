To compile: gcc -o qbi_recon src/QBI.c src	/Matrices.c src/Interpolation.c src/DirOfInterest.c src/ConvolutionAndPrediction.c src/nifti1_io.c src/znzlib.c -lm -lz

To run: ./qbi_recon -data qbidata/data.nii.gz -bvec qbidata/data.bvec -mask qbidata/cc.nii.gz -ndir 5 -odir /home/msawtell/Documents/QBI/qbidata -datadir /home/msawtell/Documents/QBI

mow_recon -data qbidata/data.nii.gz -bval qbidata/data.bval -bvec qbidata/data.bvec -mask qbidata/cc.nii.gz -ndir 5 -delta_lg 38.04 -delta_sm 25.44 -radius 8 -odir /home/msawtell/Documents/qbidata -datadir /home/msawtell/Documents/tract


Create the white matter streamlines with the following command:
track_tracker -o cc.trk -d qbidata/ -sdm qbidata/cc.nii.gz -sd 1 -so -fam qbidata/DTI_FA.nii.gz -fa .01

Create the connectivity file with the following TWO commands:
track_network -save-matching -conn_method C -m pass cc.trk sub-234_dwi_network.nii.gz cc_networkC.trk

track_network -save-matching -conn_method H -m pass cc.trk sub-234_dwi_network.nii.gz cc_networkH.trk

Next issue: Outputted files still showing the same thing. Since the values for the ODF maxima are different, I think that the issue might lie in actually saving the output data

ASK ABOUT ERROR IN TRACK_TRACKER: Warning: Orientation mismatch detected in direction files:

rename sphere.yeet to something that isn't dumb

should be able to determine exactly what the maximas are in the computeODF function, 



